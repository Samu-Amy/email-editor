import { TextComponent } from "@/lib/store";
import { createElement } from "react";

export const TextComp = ({id, text, type, size, jusify, alignment, children}: TextComponent) => {
    return createElement(type, )
}